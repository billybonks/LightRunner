#include "Segment.h"

QuadSegment::QuadSegment(){
	B2Segment::B2Segment();
}
 void QuadSegment::generate(){
	 //for now, make sure body->userdata is set to null when this is implemented.
	}

 	void QuadSegment::setPosition(const CCPoint& newPosition){

	}